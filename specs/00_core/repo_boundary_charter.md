# Repo Boundary Charter

`data-contracts-runner` is the canonical shared runner contract surface.

This repository MUST contain only non-implementation-specific runner docs/specs.

Implementation-specific runner behavior MUST be owned by `dc-runner-rust`, `dc-runner-python`, and `dc-runner-php`.
