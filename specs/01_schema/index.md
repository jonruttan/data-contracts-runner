# Schema Index

This repository reuses canonical contract schema authority from `data-contracts` and does not redefine core contract schema.

Local schema surfaces in this repo are limited to shared-runner governance metadata contracts.
